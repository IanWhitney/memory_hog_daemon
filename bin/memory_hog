#!/usr/bin/env ruby
#
# Stub executable for memory_hog
#

require File.expand_path('../../config/environment',  __FILE__)

DaemonKit::Application.exec( DAEMON_ROOT + '/libexec/memory_hog-daemon.rb' )
